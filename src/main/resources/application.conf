HttpServerConfig {
  # The port to listen on.
  port = 8080
  port = ${?PORT}

  # The hostname to listen on.
  host = "localhost"
  host = ${?HOST}

  sourceFilePath = ["LEGACY_HISTORICAL", "./src/test/resources/marmot_day_test1.tsv"]
  path = []
}